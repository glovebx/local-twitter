package dependencies

object Libraries {

    private const val image_cropper_version = "3.2.2"
    const val image_cropper = "com.github.CanHub:Android-Image-Cropper:${image_cropper_version}"

    private const val cookie_jar_version = "v1.0.1"
    const val persistent_cookie_jar = "com.github.franmontiel:PersistentCookieJar:${cookie_jar_version}"
}